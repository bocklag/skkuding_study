version: '3'
services:
  express:
    build:
      context: ./server
    container_name: express
    expose:
      - '3000'
    volumes:
      - ./source:/source
      - /source/node_modules
    restart: 'unless-stopped'